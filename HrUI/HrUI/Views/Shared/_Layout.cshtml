<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>

    <link rel="stylesheet" href="https://bootswatch.com/5/united/bootstrap.css">

</head>
<body>
   
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            @Html.ActionLink("HRMS", "Index", "Home", new { area = "Common" }, new { @class = "navbar-brand" })
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        @Html.ActionLink("Home", "Index", "Home", new { area = "Common" }, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Employee Relations</a>
                        <div class="dropdown-menu">
                            @Html.ActionLink("Employees Registration", "create", new { Controller = "employees", Area = "Admin" }, new { @class = "dropdown-item" })
                            @Html.ActionLink("Manage Employees", "Index", new { Controller = "employees", Area = "Admin" }, new { @class = "dropdown-item" })
                        </div>

                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Self Services</a>
                        <div class="dropdown-menu">
                            @Html.ActionLink("Companies", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                            @Html.ActionLink("Job Titles", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                        </div>

                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Work Force</a>
                        <div class="dropdown-menu">
                            @Html.ActionLink("Companies", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                            @Html.ActionLink("Job Titles", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                        </div>

                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Master Records</a>
                        <div class="dropdown-menu">
                            @Html.ActionLink("Companies", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                            @Html.ActionLink("Job Titles", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                        </div>

                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Leave</a>
                        <div class="dropdown-menu">
                            @Html.ActionLink("Companies", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                            @Html.ActionLink("Job Titles", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                        </div>

                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Loans</a>
                        <div class="dropdown-menu">
                            @Html.ActionLink("Companies", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                            @Html.ActionLink("Job Titles", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                        </div>

                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Payroll</a>
                        <div class="dropdown-menu">
                            @Html.ActionLink("Companies", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                            @Html.ActionLink("Job Titles", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                        </div>

                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Reports</a>
                        <div class="dropdown-menu">
                            @Html.ActionLink("Companies", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                            @Html.ActionLink("Job Titles", "Index", new { Controller = "Companies", Area = "Admin" }, new { @class = "dropdown-item" })
                        </div>
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("SingOut", "SignOut", "Login", new { area = "Security" }, new { @class = "nav-link" })
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Chris</p>
        </footer>
    </div>

    <script src="https://bootswatch.com/_vendor/jquery/dist/jquery.min.js"></script>
    <script src="https://bootswatch.com/_vendor/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>